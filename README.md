## Petit projet Kanban pour revoir quelques notions et s'initier à Docker

### Une bonne partie de l'appli est codée avec de l'IA; l'intérêt était surtout qu'elle serve de prétexte pour voir le fonctionnement de Docker et le tester sur un support un peu 'fonctionnel' (avec bdd + api symfony et un front simple).
